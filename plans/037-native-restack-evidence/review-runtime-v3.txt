autoreview findings: 2
[P2] Unwrap TypeScript assertions around defaultProps literals
packages/vite-plugin-react-docgen-typescript/src/docgen/nativeBackend.ts:662
A valid assignment such as `Component.defaultProps = { label: "x" } as Partial<Props>` (or using `satisfies`) has an `AsExpression`/`SatisfiesExpression` on the right, so this check rejects it even though its underlying expression is an object literal. The previous implementation parsed flat asserted literals because the object immediately followed `=`, and these defaults now disappear, leaving incorrect `defaultValue` and `required` metadata. Unwrap transparent expression wrappers before checking and reading the object literal.

[P2] Preserve defaultProps assignments nested in statements
packages/vite-plugin-react-docgen-typescript/src/docgen/nativeBackend.ts:653
The new parser examines only direct `sourceFile.statements`, so a valid assignment inside a block, such as a development guard around `Component.defaultProps = { ... }`, is never visited. The previous source scan found such assignments anywhere in the file, making this an observable regression in generated defaults and requiredness. Traverse the relevant AST statements rather than limiting the search to the source file's immediate children.

overall: patch is incorrect (0.92)
The source-isolation and dependency-tracking changes appear sound, but the AST conversion narrows the accepted defaultProps syntax. It drops asserted object literals and assignments nested beneath top-level statements, both of which the replaced source scan could recognize.

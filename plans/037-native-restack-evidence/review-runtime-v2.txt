autoreview findings: 5
[P2] Honor source overrides for standalone native programs
packages/vite-plugin-react-docgen-typescript/src/docgen/nativeBackend.ts:1323
When no tsconfig-backed snapshot is used (including configurations that supply `compilerOptions`), `snapshot` is undefined, so this branch analyzes the existing program source file even when `input.source` differs. The backend therefore generates documentation from disk contents rather than the source Vite passed to the transform. Apply an equivalent temporary update for standalone programs or otherwise construct the analysis from `input.source`.

[P2] Track dependencies introduced by temporary source updates
packages/vite-plugin-react-docgen-typescript/src/docgen/nativeBackend.ts:1329
Dependencies from a temporary snapshot are filtered against `trackedFiles`, which describes only the persistent project. If an in-memory source override introduces an import—especially a package declaration not loaded by the base project—the temporary analysis can use that declaration but it is discarded here, so the plugin never watches it and later edits leave cached docgen stale. Classify and retain non-default-library dependencies from the temporary project before returning the result.

[P2] Do not expand ordinary unions for the enum-only option
packages/vite-plugin-react-docgen-typescript/src/docgen/nativeBackend.ts:539
Combining `shouldExtractValuesFromUnion` and `shouldExtractLiteralValuesFromEnum` makes the enum-only option expand every literal union. For example, with only `shouldExtractLiteralValuesFromEnum: true`, a plain `"a" | "b"` prop is emitted as an enum instead of retaining its normal type representation, unlike the established parser option contract. Gate ordinary union extraction exclusively on `shouldExtractValuesFromUnion` and detect enum literals separately.

[P2] Parse defaultProps structurally instead of splitting source text
packages/vite-plugin-react-docgen-typescript/src/docgen/nativeBackend.ts:700
Splitting the object body on commas corrupts common valid defaults such as `Component.defaultProps = { label: "hello, world" }`; the recorded value becomes the truncated string `"hello`. The preceding non-greedy brace regex also truncates nested object defaults. Since this directly produces incorrect docgen metadata for supported function components, derive these properties and initializers from the AST rather than regex/string splitting.

[P2] Preserve the public export name for aliased components
packages/vite-plugin-react-docgen-typescript/src/docgen/nativeBackend.ts:918
For `const local = ...; export { local as Button }`, eligibility correctly uses the exported name and the runtime target correctly uses `local`, but the default display name is taken from the resolved local symbol. This emits `local` rather than the public component name `Button`; the resolver is likewise invoked with the local-symbol facade. Keep the local symbol only for runtime targeting/type resolution and use the export symbol for public component naming and resolver semantics.

overall: patch is incorrect (0.97)
The native backend violates several existing analysis and metadata contracts: standalone analyses can ignore the supplied source, temporary imports are not watched, an enum-specific option changes ordinary unions, structured default values are parsed incorrectly, and aliased exports lose their public names. These can produce stale or incorrect docgen output in the newly introduced native mode.

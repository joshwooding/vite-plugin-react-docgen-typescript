# Plan 033: Reuse canonical dependency output inside the backend

Status: DONE — implementation, correctness, exact-artifact compatibility and all 24 measured samples pass; both Salt modes meet the benefit rule. External review passed with no findings. Main build/typecheck and four regression cases pass with identical distribution bytes. Integrated at unsigned local source commit `09039b9f32bab728b2418b24e74636f93703b998`. See [verification](033-verification.md). Authorized by the goal to finish the performance improvements and plans. Planned at `eb7a7650a3541d08630ed60d4e684f495123d343`, 2026-09-06. Depends on completed Plans 029 and 032. Priority P2; effort M; risk MED; category performance.

Closeout authorization: after the configured signer timed out, the user explicitly requested an unsigned local commit and no push. This overrides only the configured-signing requirement in the original workflow below. The per-command override leaves persistent Git configuration unchanged. The original protocol and baseline descriptions are retained for provenance.

## Why and current contract

At the planned base, the post-029 plugin spent 46.92–49.69% of weighted Salt cold/shared-HMR samples in native realpath under `resolvePhysicalPath -> normalizeBoundaryPath -> normalizeBoundaryPaths -> analyze`. This is historical diagnostic attribution, not a profile of the final 033 artifact. The consumer is pinned Salt core at 2e1da8e4fbc398b2a7dfffbd357feedf222f7e07, all 215 TSX targets, 201 metadata-bearing files and 221 components.

In packages/vite-plugin-react-docgen-typescript/src/docgen/legacyBackend.ts, `syncFiles` at 1033 normalizes membership before copying into projectTrackedFiles. `collectDirectTrackedFileDependencies` at 712 accepts only exact names in that set. `collectTrackedFileDependencies` at 720 seeds traversal with path.resolve(entryFileName) and ambient names filtered through the same set, follows these canonical direct edges, and returns a fresh sorted Set expansion. Both callers already normalize the request entry: prepareCacheValidation at 1431 and analyze at 1462. Nevertheless, prepareCacheValidation wraps collection in normalizeBoundaryPaths at 1439, successful analysis does so at 1505, and error analysis at 1540.

Prove that this output is canonical for its active Program/project snapshot, then remove only this redundant final pass. Keep fresh array/Set ownership, deduplication, stable ordering, complete dependency graph, revisions and every actual boundary normalization. Do not implement another cache, normalize fewer raw compiler inputs, or weaken missing-file/alias behavior to get a speedup.

## Ownership and Git workflow

Create an isolated worktree at D:/OSS/vite-plugin-react-docgen-typescript/.yarn/.codex-worktrees/plan033/vite-plugin-react-docgen-typescript on codex/033-reuse-backend-canonical-dependencies from the exact planned commit. First inspect existing worktree state; never overwrite an existing directory or reset unrelated changes. Main's tracked working tree is clean; preserve all untracked plans and previous worktrees/evidence.

The executor owns source/test implementation, focused verification and its package artifact. Root owns the main plans/index, independent review, compatibility verification, measurement and final local integration. Do not commit until root confirms review gates, and do not push, publish, send code to an external review service or alter signing settings. The goal authorizes eventual local integration after validation; it does not authorize a release or backend migration.

In scope:
- packages/vite-plugin-react-docgen-typescript/src/docgen/legacyBackend.ts: the collector contract and its success/error/prepareCacheValidation consumers.
- packages/vite-plugin-react-docgen-typescript/src/docgen/backend.ts: only a concise internal canonical-output contract comment if needed.
- packages/vite-plugin-react-docgen-typescript/src/__tests__/backendCanonicalDependencies.test.ts (new) and/or targeted additions to backendParity.test.ts or persistentCacheFreshness.test.ts when those existing helpers are the appropriate home. Keep tests focused on real behavioral invariants.
- One patch Changeset, .changeset/reuse-backend-canonical-dependencies.md.
- plans/033-evidence/** and plans/033-verification.md for implementation proof in the isolated worktree. Root owns plans/README.md and BACKLOG.md.

Out of scope: plugin watchFiles, pathIdentity semantics, syncFiles membership cleanup, fallback candidate short-circuit, ambient/dependency narrowing, public options/types shape, cache format/versions, defaults, dependencies, benchmarks/CI defaults and fixture/oracle edits to accommodate bad output. Do not edit the existing Salt/shallow consumers; root owns them during measurement.

## Steps and gates

1. Confirm exact base and read both collector callers, all producers of canonical membership, project/config resets, error paths and validation revision guards. Write a concise provenance audit. Inspect existing tests before adding any. Relevant examples: backendParity.test.ts:703 referenced-project/ambient switching, :817 both cyclic transform orders, :1000 direct backend corpus, :1075 controlled parser errors; backendContract.test.ts:72 real alias helper and :2691 missing watcher rejection; persistentCacheFreshness.test.ts:23 real persisted alias HMR. Use the repository's Vitest helpers, temp fixtures and awaited dispose/finally cleanup.
2. Characterize the current baseline with real filesystem aliases/junctions and transitive imports, including an unrelated component. Assert exact canonical, sorted/deduplicated dependencies and matching prepareCacheValidation results. Exercise edit/delete/recreation and a reference/config reset or alias-target switch with fresh-backend metadata. Cover both stable modes. Include a controlled parser error so success/error/validation share the same canonical contract. For alias-target changes, distinguish already-unsupported baseline behavior from a regression; do not silently reduce an existing assertion or broaden the watcher design to make a test pass. If baseline behavior makes reuse unsafe, preserve the reproduction and report the specific boundary problem.
3. Remove the redundant final normalization at all three output paths only after proving provenance. A concise comment should explain the producer/membership invariant. If an output is not canonical, correct the nearest existing producer only if this remains within the owner boundary; otherwise report. Do not globally remove normalizeBoundaryPaths or add cached physical-path state.
4. Run focused real-backend/cache tests and typecheck/lint. Build from the package with ancestor unbuild. Record exact modified source hashes and all distribution hashes. Pack with existing Yarn into an owned ignored path; do not install or upgrade dependencies. Run the full suite once after the focused cases pass. Report any baseline-only failures and actual skipped checks honestly. Root will independently verify the final artifact and tests.
5. Root freezes the reviewed diff/artifact and performs unchanged ten-row TS4.3–6/Vite3–8 compatibility checks plus affected persistent restart and real native watcher cases on that artifact. Reuse pinned installed matrix environments where applicable, but install the actual candidate archive into the disposable row workspace and verify its bytes; do not count a prior artifact run as new verification.
6. Root performs paired unprofiled measurement of exact baseline (Plan 029 artifact/current source) and candidate on the same owned Salt and shallow real-React paths. Reuse the validated full semantic oracles and frozen workload identities with explicit artifact-location adaptation. Three fresh-process pairs per stable mode per workload: 24 total processes, sequential after heavy tests. Order by round: default AB/ProjectService BA, then default BA/ProjectService AB, then default AB/ProjectService BA. A is current baseline, B candidate. Measure cold, warm, component HMR, shared HMR and close exactly as Plan 029. Require full metadata and exact affected target sets at every phase, including unchanged-source warm. No dropped/replaced successful samples or variance extension. Each child has a 20-minute timeout and guarded owned-edit restoration. Preserve all setup/failure attempts.
7. Useful Salt benefit requires >=10% and >=100 ms median reduction in cold and shared HMR in each mode, all three paired directions positive. MAD >20% or conflicting directions makes that phase inconclusive. Flag component/session regression >10% and >100 ms, or warm >10 ms. Shallow flags cold/HMR/session regression >10% and >20 ms. Report all raw values, paired deltas and inconclusive phases. Do not claim overall performance completion if this candidate has not been verified against the declared behavior and benefit gates.
8. After source/artifact freeze and required correctness checks, root runs structured autoreview with the scoped complete diff and relevant context. Respect existing external-upload authorization scope; prepare the concrete bundle first if new permission is required. Resolve only actionable in-scope findings, then rerun affected tests and review if code changes. No extra review once a clean run exits 0. Integrate locally with configured signing after all gates pass and record exact commit/artifact evidence.

## Exact local commands

All commands use PowerShell with login disabled and the current main installation; no install is required for source checks.

- Drift: git diff --stat eb7a7650a3541d08630ed60d4e684f495123d343..HEAD -- packages/vite-plugin-react-docgen-typescript/src
- Focused: node D:/OSS/vite-plugin-react-docgen-typescript/node_modules/vitest/vitest.mjs run packages/vite-plugin-react-docgen-typescript/src/__tests__/backendCanonicalDependencies.test.ts packages/vite-plugin-react-docgen-typescript/src/__tests__/backendParity.test.ts packages/vite-plugin-react-docgen-typescript/src/__tests__/persistentCacheFreshness.test.ts --exclude '**/.yarn/**' --pool=threads --maxWorkers=2 (omit the new file argument if additions use existing suites instead).
- Full: node D:/OSS/vite-plugin-react-docgen-typescript/node_modules/vitest/vitest.mjs run --exclude '**/.yarn/**' --pool=threads --maxWorkers=2 (current baseline 339 tests/13 files; meaningful added cases raise that count).
- Types: node D:/OSS/vite-plugin-react-docgen-typescript/node_modules/typescript6/bin/tsc6 --noEmit -p packages/vite-plugin-react-docgen-typescript/tsconfig.json
- Lint: node D:/OSS/vite-plugin-react-docgen-typescript/node_modules/@biomejs/biome/bin/biome ci CHANGED_SOURCE_FILES
- Build from packages/vite-plugin-react-docgen-typescript: node D:/OSS/vite-plugin-react-docgen-typescript/node_modules/unbuild/dist/cli.mjs
- Pack from worktree root: node D:/OSS/vite-plugin-react-docgen-typescript/.yarn/releases/yarn-4.13.0.cjs workspace @joshwooding/vite-plugin-react-docgen-typescript pack --out ABSOLUTE_OWNED_TGZ
- Whitespace: git diff --check

## Completion and stop conditions

Complete only when the canonical contract, real alias/lifecycle/error/validation checks, full suite, typecheck/lint, exact-artifact compatibility, declared measurement benefit, clean structured review and local integration evidence all pass. The executor's initial handoff may end after step 4 because root owns subsequent gates; it must not label the whole plan complete.

Stop a proposed implementation that needs a new graph/cache/protocol, broad filesystem watches, weaker freshness, dependency or peer-range changes, or modified oracle expectations. Root will decide the next scoped action from the retained evidence. Do not interpret a bounded candidate failure as permission to substitute unrelated micro-optimizations. Preserve snapshots and all earlier evidence; no cleanup/deletion of other worktrees.

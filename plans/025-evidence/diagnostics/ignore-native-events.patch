diff --git a/scripts/verify-runtime-compatibility.mjs b/.yarn/simplification-evidence/025/diagnostic-ignore-native.mjs
index b5945e6..0fb8da0 100644
--- a/scripts/verify-runtime-compatibility.mjs
+++ b/.yarn/simplification-evidence/025/diagnostic-ignore-native.mjs
@@ -399,7 +399,7 @@ if (import.meta.hot) import.meta.hot.accept();
           server: {
             fs: { allow: [fixture.commonRoot] },
             middlewareMode: true,
-            watch: null,
+            watch: { ignored: () => true },
           },
         }),
         `${testLabel} server creation`,
@@ -655,6 +655,9 @@ if (import.meta.hot) import.meta.hot.accept();
         mode: runtimeMode,
         topology,
       };
+    } catch (error) {
+      console.error(`ORIGINAL_TOPOLOGY_ERROR ${testLabel}: ${error.stack ?? error}`);
+      throw error;
     } finally {
       activeCapture = undefined;
       if (server) {
